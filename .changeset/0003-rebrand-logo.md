---
"pan-scm-cli": patch
---

Refresh the project branding with a new cloud + command-prompt logo. The full wordmark logo is used where there is horizontal space (README banner, documentation social card) and the icon-only mark is used where space is tight (Docusaurus navbar, favicon).
